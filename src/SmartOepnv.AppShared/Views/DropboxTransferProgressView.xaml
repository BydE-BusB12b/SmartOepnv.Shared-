<UserControl x:Class="SmartOepnv.AppShared.Views.DropboxTransferProgressView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root"
             Visibility="Collapsed">
    <StackPanel>
        <TextBlock x:Name="PhaseText"
                   TextWrapping="Wrap"
                   TextAlignment="Center"
                   FontSize="12"
                   Opacity="0.85"
                   Foreground="White"
                   Margin="0,0,0,8" />
        <ProgressBar x:Name="TransferProgressBar"
                     Minimum="0"
                     Maximum="100"
                     Height="10"
                     Margin="0,0,0,6" />
        <Grid>
            <TextBlock x:Name="PercentText"
                       HorizontalAlignment="Left"
                       FontSize="13"
                       FontWeight="SemiBold"
                       Foreground="White" />
            <TextBlock x:Name="EtaText"
                       HorizontalAlignment="Right"
                       FontSize="12"
                       Opacity="0.85"
                       Foreground="White" />
        </Grid>
    </StackPanel>
</UserControl>
