<UserControl x:Class="SmartOepnv.AppShared.Views.PlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <StackPanel MaxWidth="720" Margin="8">
        <TextBlock x:Name="TitleText"
                   Style="{StaticResource SmartPageTitleStyle}" />
        <TextBlock x:Name="DescriptionText"
                   Style="{StaticResource SmartPageSubtitleStyle}" />
        <materialDesign:Card Padding="24"
                             UniformCornerRadius="12"
                             Background="{StaticResource SmartSurfaceBrush}">
            <StackPanel>
                <materialDesign:PackIcon Kind="HammerScrewdriver"
                                       Width="48" Height="48"
                                       HorizontalAlignment="Left"
                                       Foreground="{StaticResource SmartAccentBrush}"
                                       Margin="0,0,0,16" />
                <TextBlock Text="Modul in Vorbereitung"
                           FontSize="18"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8" />
                <TextBlock Text="Dieser Bereich wird als Nächstes mit Datenmodellen und GPSAnsagen-JSON-Anbindung implementiert."
                           TextWrapping="Wrap"
                           Opacity="0.85" />
            </StackPanel>
        </materialDesign:Card>
    </StackPanel>
</UserControl>
