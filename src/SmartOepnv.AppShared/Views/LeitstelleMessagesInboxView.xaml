<UserControl x:Class="SmartOepnv.AppShared.Views.LeitstelleMessagesInboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0" Padding="12,10" Margin="0,0,0,8" UniformCornerRadius="10"
                             Background="{StaticResource SmartSurfaceBrush}">
            <DockPanel LastChildFill="True">
                <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                    <Button Content="Jetzt aktualisieren"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Margin="0,0,10,0" />
                    <Button Content="Mail als gelesen"
                            Command="{Binding MarkMailAsReadCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}" />
                </StackPanel>
                <TextBlock DockPanel.Dock="Right"
                           Text="{Binding StatusMessage}"
                           VerticalAlignment="Center"
                           TextWrapping="Wrap"
                           Opacity="0.82"
                           MaxWidth="620" />
            </DockPanel>
        </materialDesign:Card>

        <ListBox Grid.Row="1"
                 x:Name="MessageList"
                 ItemsSource="{Binding Items}"
                 BorderThickness="0"
                 Background="Transparent"
                 ScrollViewer.VerticalScrollBarVisibility="Auto"
                 PreviewMouseLeftButtonDown="MessageList_PreviewMouseLeftButtonDown">
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem" BasedOn="{StaticResource MaterialDesignListBoxItem}">
                    <Setter Property="Padding" Value="0" />
                    <Setter Property="Margin" Value="0,0,0,6" />
                    <Setter Property="Cursor" Value="Hand" />
                    <Setter Property="ToolTip" Value="Klicken: Live-Karte mit Fahrzeug-Info öffnen" />
                </Style>
            </ListBox.ItemContainerStyle>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="10,8" CornerRadius="8" BorderBrush="#2E3B4E" BorderThickness="1">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Setter Property="Background" Value="{StaticResource SmartSurfaceBrush}" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsSos}" Value="True">
                                        <Setter Property="Background" Value="#55B71C1C" />
                                        <Setter Property="BorderBrush" Value="#FFCDD2" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsUnread}" Value="True">
                                        <Setter Property="Background" Value="#2DFFF59D" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="{Binding VehicleName}" FontWeight="SemiBold" FontSize="14" />
                                    <TextBlock Text="{Binding PhoneNormalized, StringFormat= ({0})}" Margin="6,0,0,0" Opacity="0.72" />
                                </StackPanel>
                                <TextBlock Text="{Binding Message}" Margin="0,4,0,0" TextWrapping="Wrap" />
                            </StackPanel>

                            <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                                <Button Content="Löschen"
                                        Command="{Binding DataContext.DeleteItemCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource MaterialDesignFlatButton}"
                                        Foreground="#EF9A9A"
                                        Padding="4,0"
                                        Margin="0,0,0,4"
                                        HorizontalAlignment="Right"
                                        Cursor="Hand"
                                        ToolTip="Nachricht aus der Liste entfernen" />
                                <TextBlock Text="{Binding TimestampLabel}" FontSize="11" Opacity="0.72" HorizontalAlignment="Right" />
                                <TextBlock Text="{Binding Type}" FontSize="11" HorizontalAlignment="Right" Margin="0,2,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Foreground" Value="#90CAF9" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSos}" Value="True">
                                                    <Setter Property="Text" Value="SOS" />
                                                    <Setter Property="Foreground" Value="#FFCDD2" />
                                                    <Setter Property="FontWeight" Value="Bold" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
