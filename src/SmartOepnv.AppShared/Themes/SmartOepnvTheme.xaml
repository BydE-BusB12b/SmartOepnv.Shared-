<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Smart-ÖPNV Planer: Dunkelblau (Material Blue 900 / 800) -->
    <Color x:Key="SmartPrimaryColor">#FF0D47A1</Color>
    <Color x:Key="SmartPrimaryDarkColor">#FF002171</Color>
    <Color x:Key="SmartPrimaryLightColor">#FF5472D3</Color>
    <Color x:Key="SmartAccentColor">#FF42A5F5</Color>
    <Color x:Key="SmartSurfaceColor">#FF121A2E</Color>
    <Color x:Key="SmartBackgroundColor">#FF0A1020</Color>

    <SolidColorBrush x:Key="SmartPrimaryBrush" Color="{StaticResource SmartPrimaryColor}" />
    <SolidColorBrush x:Key="SmartPrimaryDarkBrush" Color="{StaticResource SmartPrimaryDarkColor}" />
    <SolidColorBrush x:Key="SmartPrimaryLightBrush" Color="{StaticResource SmartPrimaryLightColor}" />
    <SolidColorBrush x:Key="SmartAccentBrush" Color="{StaticResource SmartAccentColor}" />
    <SolidColorBrush x:Key="SmartSurfaceBrush" Color="{StaticResource SmartSurfaceColor}" />
    <SolidColorBrush x:Key="SmartBackgroundBrush" Color="{StaticResource SmartBackgroundColor}" />

    <Color x:Key="SmartSuccessColor">#FF66BB6A</Color>
    <Color x:Key="SmartSuccessBackgroundColor">#264CAF50</Color>
    <Color x:Key="SmartSuccessBorderColor">#664CAF50</Color>
    <Color x:Key="SmartSuccessOnDarkColor">#FF81C784</Color>
    <Color x:Key="SmartSuccessOnDarkBackgroundColor">#4081C784</Color>
    <Color x:Key="SmartSuccessOnDarkBorderColor">#8081C784</Color>

    <SolidColorBrush x:Key="SmartSuccessForegroundBrush" Color="{StaticResource SmartSuccessColor}" />
    <SolidColorBrush x:Key="SmartSuccessBackgroundBrush" Color="{StaticResource SmartSuccessBackgroundColor}" />
    <SolidColorBrush x:Key="SmartSuccessBorderBrush" Color="{StaticResource SmartSuccessBorderColor}" />
    <SolidColorBrush x:Key="SmartSuccessOnDarkForegroundBrush" Color="{StaticResource SmartSuccessOnDarkColor}" />
    <SolidColorBrush x:Key="SmartSuccessOnDarkBackgroundBrush" Color="{StaticResource SmartSuccessOnDarkBackgroundColor}" />
    <SolidColorBrush x:Key="SmartSuccessOnDarkBorderBrush" Color="{StaticResource SmartSuccessOnDarkBorderColor}" />

    <Style x:Key="SmartPageTitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="28" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource MaterialDesign.Brush.Primary.Light}" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>

    <Style x:Key="SmartPageSubtitleStyle" TargetType="TextBlock">
        <Setter Property="FontSize" Value="14" />
        <Setter Property="Opacity" Value="0.75" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Margin" Value="0,0,0,24" />
    </Style>

    <!-- Dunkle ComboBox (Dropdown + Scrollbar) für Dialoge auf SmartPrimaryDark -->
    <Style x:Key="SmartDarkComboBoxItem" TargetType="ComboBoxItem">
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Background" Value="{StaticResource SmartPrimaryDarkBrush}" />
        <Setter Property="Foreground" Value="White" />
        <Setter Property="HorizontalContentAlignment" Value="Left" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd"
                            Background="{TemplateBinding Background}"
                            BorderThickness="0"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{StaticResource SmartPrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Foreground" Value="#88FFFFFF" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="SmartDarkComboBoxScrollBar" TargetType="ScrollBar">
        <Setter Property="Background" Value="#FF001433" />
        <Setter Property="Width" Value="12" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ScrollBar">
                    <Grid Background="{TemplateBinding Background}">
                        <Track x:Name="PART_Track" IsDirectionReversed="True">
                            <Track.Thumb>
                                <Thumb>
                                    <Thumb.Template>
                                        <ControlTemplate TargetType="Thumb">
                                            <Border Background="{StaticResource SmartAccentBrush}"
                                                    CornerRadius="4"
                                                    Margin="2" />
                                        </ControlTemplate>
                                    </Thumb.Template>
                                </Thumb>
                            </Track.Thumb>
                        </Track>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="Orientation" Value="Horizontal">
                <Setter Property="Height" Value="12" />
                <Setter Property="Width" Value="Auto" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <ControlTemplate x:Key="SmartDarkComboBoxToggleTemplate" TargetType="ToggleButton">
        <Border x:Name="Bd"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                SnapsToDevicePixels="True">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="20" />
                </Grid.ColumnDefinitions>
                <ContentPresenter Margin="{TemplateBinding Padding}"
                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                  IsHitTestVisible="False"
                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                <Path Grid.Column="1"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Data="M 0 0 L 4 4 L 8 0 Z"
                      Fill="White" />
            </Grid>
        </Border>
        <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="{StaticResource SmartPrimaryBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter TargetName="Bd" Property="Opacity" Value="0.5" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <ControlTemplate x:Key="SmartDarkComboBoxTemplate" TargetType="ComboBox">
        <Grid SnapsToDevicePixels="True">
            <ToggleButton x:Name="ToggleButton"
                          Background="{TemplateBinding Background}"
                          BorderBrush="{TemplateBinding BorderBrush}"
                          BorderThickness="{TemplateBinding BorderThickness}"
                          Foreground="{TemplateBinding Foreground}"
                          Focusable="False"
                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                          ClickMode="Press"
                          Padding="{TemplateBinding Padding}"
                          Template="{StaticResource SmartDarkComboBoxToggleTemplate}">
                <ContentPresenter Content="{TemplateBinding SelectionBoxItem}"
                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                  HorizontalAlignment="Left"
                                  VerticalAlignment="Center"
                                  IsHitTestVisible="False" />
            </ToggleButton>
            <Popup x:Name="PART_Popup"
                   AllowsTransparency="True"
                   Placement="Bottom"
                   IsOpen="{TemplateBinding IsDropDownOpen}"
                   PopupAnimation="Slide"
                   Focusable="False">
                <Grid MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}"
                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                    <Border Background="{StaticResource SmartPrimaryDarkBrush}"
                            BorderBrush="{StaticResource SmartAccentBrush}"
                            BorderThickness="1"
                            CornerRadius="0,0,2,2" />
                    <ScrollViewer Margin="1"
                                  SnapsToDevicePixels="True"
                                  HorizontalScrollBarVisibility="Disabled"
                                  VerticalScrollBarVisibility="Visible"
                                  CanContentScroll="True">
                        <ScrollViewer.Resources>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource SmartDarkComboBoxScrollBar}" />
                        </ScrollViewer.Resources>
                        <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
                    </ScrollViewer>
                </Grid>
            </Popup>
        </Grid>
        <ControlTemplate.Triggers>
            <Trigger Property="HasItems" Value="False">
                <Setter TargetName="PART_Popup" Property="MinHeight" Value="32" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.55" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="SmartDarkComboBox" TargetType="ComboBox">
        <Setter Property="Foreground" Value="White" />
        <Setter Property="Background" Value="#FF1E5A9E" />
        <Setter Property="BorderBrush" Value="{StaticResource SmartAccentBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="MinHeight" Value="32" />
        <Setter Property="MaxDropDownHeight" Value="260" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Visible" />
        <Setter Property="ItemContainerStyle" Value="{StaticResource SmartDarkComboBoxItem}" />
        <Setter Property="Template" Value="{StaticResource SmartDarkComboBoxTemplate}" />
        <Setter Property="Resources">
            <Setter.Value>
                <ResourceDictionary>
                    <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="#FF002171" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.WindowTextBrushKey}" Color="White" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF0D47A1" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlBrushKey}" Color="#FF1E5A9E" />
                    <SolidColorBrush x:Key="{x:Static SystemColors.ControlTextBrushKey}" Color="White" />
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource SmartDarkComboBoxScrollBar}" />
                </ResourceDictionary>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
